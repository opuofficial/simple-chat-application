import React from "react";

const SelectConversation = () => {
  return (
    <div
      className="flex justify-center items-center p-3 text-3xl"
      style={{ height: `calc(100vh - 64px)` }}
    >
      Select a Conversation
    </div>
  );
};

export default SelectConversation;
